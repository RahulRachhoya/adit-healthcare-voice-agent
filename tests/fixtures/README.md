# Synthetic test data

Reusable fixtures are defined in `tests/conftest.py`. JSON examples under
`examples/` and `evaluations/` are synthetic, including any purported recording
reference. They are not real call evidence.
